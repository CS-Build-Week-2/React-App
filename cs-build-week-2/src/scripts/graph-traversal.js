import Axios from "axios";

// call HTTP request each iteration of the graph traversal

function traverseGraph(userToken) {
    
    // initial conditions
    visited = []
    newGraph = { startingRoom: {"n": "?", "s": "?", "w": "?", "e": "?"} }
    
    currentRoom = axiso.get("")
    // step 1 
    // each time we call the axios call, we do depth first search
    // until the end 
    while (len(visited) < 500) {

    }

    // step 2 
    // each time we visit a room, the previous room data is stored 
    // inside the local storage so that
    
    // when the next room is visited, then we can place that room 
    // inside the visited array []

    // step 3 
    // and then we do breadth first search to find the nearest exit
    // until we discovered a room we haven't visited yet  

}